export type PetsDTO = {
    id: string
    name:  string
    idade: string
    peso: string
    porte: string
    tipo: string
    descricao: string
    requisitos: string
    fotos: string | null,
    userId: string,
    costumerId: string
}